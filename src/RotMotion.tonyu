firstX;firstY;
sp;radius;
angle;

\next(){
    var pos = { x=0, y=0 };
    
    if( firstX != null && firstY != null ){
        angle += sp;
        pos.x = firstX + cos( angle ) * radius;
        pos.y = firstY + sin( angle ) * radius; 
    }
    
    return pos;
}