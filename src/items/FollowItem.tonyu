getRandItem = new GetRandItem;

if( badItem === false ){
    if( itemType === 0 ) p = getRandItem.getBottoms();
    if( itemType === 1 ) p = getRandItem.getTops();
    if( itemType === 2 ) p = getRandItem.getHat();
}else if( badItem === true ){
    if( itemType === 0 ) p = $pat_bad_zubon_in;
    if( itemType === 1 ) p = $pat_bad_autaa_in;
    if( itemType === 2 ) p = $pat_bad_bousi_in;
}

scaleX = $W * 0.0008;

while( true ){
    x = player.x;
    y = player.y;
    
    update();
}