firstX;firstY;
sp;
angle = 1;
radCos = $W * 0.5;

\next( targetAct ){
    angle += sp;
    pos = { x=0, y=0 };
    
    if( firstX && firstY ){
        pos.x = firstX + cos( angle ) * radCos;
        pos.y = firstY;  
    }
    
    return pos;
}