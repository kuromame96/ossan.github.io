firstX;firstY;
target;
badItem;itemType;
base = new Items{
    x=firstX, y=firstY, scaleX=$scrScale * 0.5,
    itemType, badItem=badItem,
};
mot = new AttractionMotion{sp=5};

while( true ){
    if( !mot || !base || !target ) return;
    mot.app( base, target );
    update();
}