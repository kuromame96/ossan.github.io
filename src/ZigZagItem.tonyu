extends Items;
native Math;

angleDeg;
XSP;YSP;

super.main();

while( true ){
    angleDeg += XSP;
    
    angleRad = angleDeg * ( Math.PI / 180 ); 
    x = $W * 0.5 + Math.cos( angleRad ) * $W * 0.75;
    y += YSP;
    update();
}