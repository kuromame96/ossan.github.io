img;
p = img+0;
frame = 0;
imgNo = 0;
zOrder = 10;
attack = false;
attackTime = 0;

\attackUpdate(){
    attackTime++;
    if( attackTime === 1 ){
        $sound.playSE( $se_kira, 128, 0, 1.5 );
        p = img+2;
        new Kira{ x=x, y=y };
    }
}

while( true ){
    if( !attack ){
        frame++;
        if( !( frame % 6 ) ) imgNo++;
        p = img + imgNo % 2;
        attackTime = 0;
    }else{
        attackUpdate();
    }
    
    update();
}